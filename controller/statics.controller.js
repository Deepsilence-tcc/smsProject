/**
 * Created by cong on 2017/8/21.
 */
var CodeMessage = require('../util/codemessage');
var ResultModel = require('../util/resultmodel');

module.exports = {
    getNewest:function (req,res,next) {
        var page = req.query.page;
        if(!page){
            
        }
    }
}
